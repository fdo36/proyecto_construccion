<div class="headContent">
    <h1>Lista de las Devoluciones de Insumos</h1>
</div>
<div class="box">
    <div class="newTopBanner">
        <%= link_to 'Nueva devolucion de insumos', new_supplies_return_path, :class => "button blue alignright" %>
    </div>
    <p id="notice" class="alignleft"><%= notice %> </p>
    <table cellpadding="0" cellspacing="0" border="0" class="display datatable">
      <thead>
          <tr>
            <th>Trabajador</th>
            <th>Insumo</th>
            <th>Cantidad</th>
            <th>Acciones</th>
          </tr>
      </thead>
      <tbody>
        <% @supplies_returns.each do |supplies_return| %>
            <tr>
              <td><%= supplies_return.worker_id %></td>
              <td><%= supplies_return.supply_id %></td>
              <td><%= supplies_return.quantity %></td>
              <td><%= link_to 'Ver', supplies_return %></td>
              <td><%= link_to 'Editar', edit_supplies_return_path(supplies_return)  %></td>
              <td><%= link_to 'Eliminar', supplies_return, method: :delete, data: { confirm: 'Esta seguro que desea eliminar esta devolucion?' } %></td>
            </tr>
        <% end %>
      </tbody>
    </table>
</div>
