<% if current_user.super_admin == true %>
  <%= link_to( wt( 'Registro Histórico' ), wiki_page_history_path(@page), :class => "button green alignright") if history_allowed? %>
  <%= link_to( wt( 'Editar' ), wiki_page_edit_path(@page), :class => "button green alignright" ) if edit_allowed? %>
  <%= link_to( wt( 'Eliminar' ), wiki_page_path(@page), :class => "button red alignright", :method => :delete, :confirm => wt( "Estás seguro que deseas eliminar esta página?" ) ) if destroy_allowed? %>	
<% end %>
<%= link_to( wt( 'Página principal Ayuda' ), "/wiki", :class => "button blue alignleft")%>