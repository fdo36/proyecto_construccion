<%= form_for(@transit_chamber_io) do |f| %>
  <% if @transit_chamber_io.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transit_chamber_io.errors.count, "error") %> prohibited this transit_chamber_io from being saved:</h2>

      <ul>
      <% @transit_chamber_io.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :temperature %><br />
    <%= f.text_field :temperature %>
  </div>
  <div class="field">
    <%= f.label :order_number %><br />
    <%= f.number_field :order_number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
