<h1>Listing pallets</h1>

<table>
  <tr>
    <th>Code</th>
    <th>Quantity</th>
    <th>Price per unit</th>
    <th>Gross weight</th>
    <th>Tare</th>
    <th>Variety</th>
    <th>Quality</th>
    <th>Receipt</th>
    <th>Pack type</th>
    <th>Company</th>
    <th>Dispatch</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pallets.each do |pallet| %>
  <tr>
    <td><%= pallet.code %></td>
    <td><%= pallet.quantity %></td>
    <td><%= pallet.price_per_unit %></td>
    <td><%= pallet.gross_weight %></td>
    <td><%= pallet.tare %></td>
    <td><%= pallet.variety_id %></td>
    <td><%= pallet.quality_id %></td>
    <td><%= pallet.receipt_id %></td>
    <td><%= pallet.pack_type_id %></td>
    <td><%= pallet.company_id %></td>
    <td><%= pallet.dispatch_id %></td>
    <td><%= link_to 'Show', pallet %></td>
    <td><%= link_to 'Edit', edit_pallet_path(pallet) %></td>
    <td><%= link_to 'Destroy', pallet, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Pallet', new_pallet_path %>
