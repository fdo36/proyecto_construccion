<%= form_for(@pack_packing) do |f| %>
  <% if @pack_packing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pack_packing.errors.count, "error") %> prohibited this pack_packing from being saved:</h2>

      <ul>
      <% @pack_packing.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
