<%= form_for(@subprocess) do |f| %>
  <% if @subprocess.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subprocess.errors.count, "error") %> prohibited this subprocess from being saved:</h2>

      <ul>
      <% @subprocess.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :process_id %><br />
    <%= f.number_field :process_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
