<h1>Listing packing_pallets</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @packing_pallets.each do |packing_pallet| %>
  <tr>
    <td><%= link_to 'Show', packing_pallet %></td>
    <td><%= link_to 'Edit', edit_packing_pallet_path(packing_pallet) %></td>
    <td><%= link_to 'Destroy', packing_pallet, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Packing pallet', new_packing_pallet_path %>
